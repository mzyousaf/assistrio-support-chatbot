"use client";

/** IIFE / `mountEmbedWidget` entry — same implementation as the in-app preview widget. */
export { EmbedWidgetRoot as AssistrioSnippetRoot } from "./widget-root";
export type { EmbedWidgetRootProps as AssistrioSnippetRootProps } from "./widget-root";
