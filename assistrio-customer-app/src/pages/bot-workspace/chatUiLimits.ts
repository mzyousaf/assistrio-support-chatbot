import { BOT_FIELD_MAX } from '@/lib/botFieldLimits';

/** Footer branding line in widget appearance — keep in sync with admin/backend. */
export const CHAT_UI_BRANDING_MESSAGE_MAX_LENGTH = BOT_FIELD_MAX.brandingMessage;
/** Privacy / footer second line in widget appearance. */
export const CHAT_UI_PRIVACY_TEXT_MAX_LENGTH = BOT_FIELD_MAX.privacyText;
