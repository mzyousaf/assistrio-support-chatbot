/**
 * Shared copy for suggestion / visitor-chip forms (single source; overview + edit stay aligned).
 */

/** Section intro under “Chip text”. */
export const KB_SUGGESTION_CHIP_SECTION_INTRO =
  'Visitors tap the chip to send your label as their opening message—keep it short and conversational.';

/** Helper under the chip input (complements the section intro). */
export const KB_SUGGESTION_CHIP_FIELD_HELPER =
  'Shown on the chip in the dashboard preview and on your live widget.';

/** Section intro under “Scope”. */
export const KB_SUGGESTION_SCOPE_SECTION_INTRO =
  'Optional cap on the first reply after this chip: paste facts or wording you want the model to lean on before it searches the wider library.';

/** Helper under scoped textarea. */
export const KB_SUGGESTION_SCOPE_FIELD_HELPER =
  'Leave empty so the first reply after this chip may use any trained knowledge.';

/** Update flow: picker field. */
export const KB_SUGGESTION_PICK_TO_EDIT_HELPER =
  'Choose which chip to edit—the next screen loads its label and scoped content.';
