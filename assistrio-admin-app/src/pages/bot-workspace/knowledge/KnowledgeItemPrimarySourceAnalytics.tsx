/** Admin: primary-source analytics API not mirrored yet (customer-only). */
export function KnowledgeItemPrimarySourceAnalytics(_props: {
  botId: string;
  knowledgeItemId: string | null;
}) {
  return null;
}
