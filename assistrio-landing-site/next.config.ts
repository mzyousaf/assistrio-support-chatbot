import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Add `images.remotePatterns` entries when using https:// screenshot URLs in NEXT_PUBLIC_HOME_* env vars. */
};

export default nextConfig;
