/**
 * Primary /trial CTA — one label that carries both “try free” and “create your agent”
 * everywhere we use a single button for that path (header, footer, hero, gallery, etc.).
 */
export const TRIAL_PRIMARY_CTA_LABEL = "Try it free — create your AI Agent";

/** Free trial reassurance — use beside Explore/trial CTAs, pricing, etc. (not nav). */
export const TRIAL_NO_CREDIT_CARD_NOTE = "No credit card required";

/** Header/footer nav trial button copy (same /trial path). */
export const TRIAL_NAV_PRIMARY_LINE = "Try Your AI Agent Free";
/** Analytics `label` on nav trial CTAs — matches button text. */
export const TRIAL_NAV_STACK_LABEL = TRIAL_NAV_PRIMARY_LINE;

/** Gallery link in header/footer — short label (no “by Assistrio”). */
export const GALLERY_CTA_NAV_LABEL = "See Live AI Agents";
